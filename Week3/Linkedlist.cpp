#include <bits/stdc++.h>
using namespace std;
struct node
{
    int data;
    struct node *next;
};
struct TNODE{
    int key;
    TNODE* pLeft;
    TNODE* pRight;
};
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    struct node *head;
    struct node *one = NULL;
    struct node *two = NULL;
    struct node *three = NULL;
}